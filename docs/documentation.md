
# Configuracion 

ejecutar en el CLI

```bash
keytool -genkey -v -keystore D:/Dev_Mob_demo/key/mi_app_keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias demokey1
```
se crea un nuevo directorio a nivel de la raíz, llamado key

Despues de correr el comando, pide inregsar datos 

password: la que tu elijas
alias: demokey1 (en este caso)
